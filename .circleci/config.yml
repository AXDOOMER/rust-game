version: 2.1

orbs:
  python: rust/rust@0.1.3

jobs:
  build:
    docker:
      - image: rust:1
    steps:
      - checkout

      - run:
          name: Cargo
          command: cargo run

workflows:
  main:
    jobs:
      - build